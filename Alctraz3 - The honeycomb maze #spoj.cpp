#include <bits/stdc++.h>
using namespace std;
typedef long long int ll;
int main()
{
    ll N, M;
    ll x, y; //just random variables for some use
    cin >> N >> M; //N -> rows M -> columns
    ll k; //no. of moves
    cin >> k;
    ll n, m; // n, m -> starting points 
    cin >> n >> m;
    for(ll i=0; i<N; i++)
    {
        for(ll j=0; j<M; j++)
        {
            cin >> x;
            
        }
    }
}